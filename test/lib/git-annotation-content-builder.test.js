
const GitAnnotationContentBuilder = require('../../lib/git-annotation-content-builder');

suite('GitAnnotationContentBuilder', () => {

    // test('it retrieves annotation data and compose annotation document', () => {
    //     const builder = new GitAnnotationContentBuilder();
    //     return builder.build(lines, 'REPOSITORY_ROOT').then(document => {
    //         expect(document).to.eql('ANNOTATION_DOCUMENT');
    //     });
    // });
});
